import React, { useContext } from "react";
import { Usercontext } from "../App";

const Newcontext=()=>{
    const cons  = useContext(Usercontext)
    return(
        <>
        </>
    )
}
export default Newcontext